Strips promoted to chapter figures live here, committed individually.

A thesis figure must not depend on a regeneration step years later, so the
image itself is committed alongside the row of the sampling manifest that
produced it. Record that row's tubelet identifier in the figure caption source.
