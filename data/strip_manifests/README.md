Sampling manifests for Stage 1a items E and F.

One row per strip, with columns:
  video, tubelet_id, frame_indices, T, stride, seed, item

These are committed. The strips themselves are not. Manifest plus code plus
data regenerates any strip exactly.
